basePath: /api/v1
definitions:
  request.LoginRequest:
    description: 用户登录时提交的请求参数
    properties:
      address:
        description: 用户钱包地址
        example: F59CSoJEmjDFQWZMjSjjvu6q7xV31p9rPzRynwrE71yk
        type: string
      invite_code:
        description: 邀请码（可选）
        example: INVITE123
        type: string
      message:
        description: 登录消息
        example: "Sign in to the super.exchange\r\n\r\nTimestamp: 1740885327"
        type: string
      signature:
        description: 签名信息
        example: Pli28P7PHx6Mzh+RwRTcqtCSuNs2qlncAqC4tK9PFN/CLnvZ2Gm8koqvkLPEZNoiVJOX71hCBaQV9NUKVX8KDg==
        type: string
      timestamp:
        description: 时间戳
        example: "1740885327"
        type: string
    type: object
  response.GetTickerResponse:
    description: Ticker 的具体数据
    properties:
      market:
        allOf:
        - $ref: '#/definitions/response.Market'
        description: 市场信息
      market_metadata:
        allOf:
        - $ref: '#/definitions/response.MarketMetadata'
        description: 市场元数据
      market_ticker:
        allOf:
        - $ref: '#/definitions/response.MarketTicker'
        description: 市场行情
    type: object
  response.InviteCodeResponse:
    properties:
      invite_code:
        description: 邀请码
        example: GT4L5B
        type: string
      invite_count:
        description: 邀请数量
        example: 0
        type: integer
    type: object
  response.InvitedPointsDetail:
    properties:
      fee_rebate:
        type: string
      invite_time:
        type: integer
      invitee:
        type: string
      trading_points:
        type: string
      update_time:
        type: integer
    type: object
  response.InvitedPointsTotalResponse:
    properties:
      cursor:
        type: integer
      details:
        items:
          $ref: '#/definitions/response.InvitedPointsDetail'
        type: array
      has_more:
        type: boolean
    type: object
  response.KlineData:
    properties:
      close:
        type: number
      high:
        type: number
      low:
        type: number
      open:
        type: number
      timestamp:
        type: integer
      volume:
        type: number
    type: object
  response.LoginResponse:
    description: 登录成功后返回的具体数据
    properties:
      expire_time:
        description: 邀请码
        example: ""
        type: string
      token:
        description: 登录令牌
        example: _vetZFMHLOlPWer8_uTrqe5_7gGGNPi-fvD--pomTdSd2kn3po2uBujC4o7raUiNHPpKJaS_BR3jmgOEdaAEFA==
        type: string
      user:
        allOf:
        - $ref: '#/definitions/response.UserInfo'
        description: 用户信息
    type: object
  response.Market:
    description: 市场的基本信息
    properties:
      create_timestamp:
        example: 1740033455
        type: integer
      creator:
        description: 创建者地址
        example: 3P3PMv28AM7SvNkGmAdHCusPwahSB9QG9z5o4gTvmNBX
        type: string
      decimals:
        description: 代币铸造地址
        example: 6
        type: integer
      market:
        description: 市场地址
        example: B3o198GX45DZ9HeJZ56wuruyWWCfGVBSn5qC71q5AHZz
        type: string
      market_id:
        description: 市场ID
        example: 1
        type: integer
      native_vault:
        description: 原生代币金库地址
        example: 98J2RACW3wk8u36k1cPmDenzHdPjhcd5qXsm5g471kdh
        type: string
      price:
        description: 当前价格
        example: 5.0417320729e-05
        type: number
      token_mint:
        description: 代币铸造地址
        example: suprkbfvwpFZXzWaoKjTzGkW1nkvvwK9n2E6g1zyLFo
        type: string
      token_name:
        description: 代币名称
        example: §uper Exchange
        type: string
      token_symbol:
        description: 代币符号
        example: SUPER
        type: string
      token_vault:
        description: 代币金库地址
        example: 9SQy9T4pzadupDcWdYYgFmUgXLVydfQwgKZEbzeACNAQ
        type: string
      uri:
        description: URI
        example: https://static.super.exchange/m/official/super.json
        type: string
    type: object
  response.MarketMetadata:
    description: 市场的元数据信息
    properties:
      banner:
        description: 横幅图片
        example: ""
        type: string
      description:
        description: 描述
        example: ""
        type: string
      github:
        type: string
      image_url:
        description: 图片URL
        example: https://static.super.exchange/i/official/super.png
        type: string
      rules:
        description: 规则
        example: ""
        type: string
      sort:
        description: 排序
        example: 0
        type: integer
      telegram:
        description: Telegram链接
        example: https://t.me/SuperExchangeCommunity
        type: string
      twitter:
        description: Twitter链接
        example: https://x.com/_superexchange
        type: string
      website:
        description: 网站链接
        example: https://super.exchange
        type: string
    type: object
  response.MarketResponse:
    properties:
      market:
        allOf:
        - $ref: '#/definitions/response.Market'
        description: 市场数据
      market_metadata:
        allOf:
        - $ref: '#/definitions/response.MarketMetadata'
        description: 市场元数据
      market_ticker:
        allOf:
        - $ref: '#/definitions/response.MarketTicker'
        description: 市场 Ticker 数据
    type: object
  response.MarketTicker:
    description: 市场的行情数据
    properties:
      buy_token_volume_1h:
        example: "379231.972430"
        type: string
      buy_token_volume_1h_usd:
        example: "379231.972430"
        type: string
      buy_token_volume_5m:
        example: "62269.324250"
        type: string
      buy_token_volume_5m_usd:
        example: "62269.324250"
        type: string
      buy_token_volume_24h:
        example: "15435782.918031"
        type: string
      buy_token_volume_24h_usd:
        example: "15435782.918031"
        type: string
      buy_token_volume_30m:
        example: "62269.324250"
        type: string
      buy_token_volume_30m_usd:
        example: "62269.324250"
        type: string
      buy_tx_count_1h:
        example: 271
        type: integer
      buy_tx_count_5m:
        example: 25
        type: integer
      buy_tx_count_24h:
        example: 7566
        type: integer
      buy_tx_count_30m:
        example: 25
        type: integer
      holders:
        example: 4204
        type: integer
      last_swap_at:
        example: 1740887399
        type: integer
      market_cap:
        example: "50417.320729000000000"
        type: string
      price:
        description: 当前价格
        example: 5.0417320729e-05
        type: number
      price_change_1h:
        example: "0.005103"
        type: string
      price_change_5m:
        example: "0.000836"
        type: string
      price_change_24h:
        example: "0.224965"
        type: string
      price_change_30m:
        example: "0.000836"
        type: string
      rank:
        example: 1
        type: integer
      sell_token_volume_1h:
        example: "379231.972430"
        type: string
      sell_token_volume_1h_usd:
        example: "379231.972430"
        type: string
      sell_token_volume_5m:
        example: "62269.324250"
        type: string
      sell_token_volume_5m_usd:
        example: "62269.324250"
        type: string
      sell_token_volume_24h:
        example: "15435782.918031"
        type: string
      sell_token_volume_24h_usd:
        example: "15435782.918031"
        type: string
      sell_token_volume_30m:
        example: "62269.324250"
        type: string
      sell_token_volume_30m_usd:
        example: "62269.324250"
        type: string
      sell_tx_count_1h:
        example: 271
        type: integer
      sell_tx_count_5m:
        example: 25
        type: integer
      sell_tx_count_24h:
        example: 7566
        type: integer
      sell_tx_count_30m:
        example: 25
        type: integer
      token_volume_1h:
        example: "379231.972430"
        type: string
      token_volume_1h_usd:
        example: "379231.972430"
        type: string
      token_volume_5m:
        example: "62269.324250"
        type: string
      token_volume_5m_usd:
        example: "62269.324250"
        type: string
      token_volume_24h:
        example: "15450301.872331"
        type: string
      token_volume_24h_usd:
        example: "15450301.872331"
        type: string
      token_volume_30m:
        example: "62269.324250"
        type: string
      token_volume_30m_usd:
        example: "62269.324250"
        type: string
      tx_count_1h:
        example: 271
        type: integer
      tx_count_5m:
        example: 25
        type: integer
      tx_count_24h:
        example: 7590
        type: integer
      tx_count_30m:
        example: 25
        type: integer
    type: object
  response.Moderator:
    description: 持有者的管理员信息
    properties:
      banned:
        description: 是否被封禁
        example: false
        type: boolean
      banned_mod_id:
        description: 被封禁的 Moderator ID
        example: 0
        type: integer
      status:
        description: 当前状态
        example: NORMAL
        type: string
    type: object
  response.MyInfoResponse:
    description: 包含用户基本信息、社交统计、邀请码等详细信息。
    properties:
      fan_count:
        description: 关注数量
        type: integer
      follow_status:
        description: 关注状态
        type: string
      follower_count:
        description: 粉丝数量
        type: integer
      has_bound:
        description: 是否已绑定
        type: boolean
      invite_code:
        description: 邀请码
        type: string
      inviter_id:
        type: integer
      mention_count:
        description: 提及数量
        type: integer
      parent_inviter_id:
        type: integer
      user:
        allOf:
        - $ref: '#/definitions/response.UserInfo'
        description: 用户基本信息
      vote_count:
        description: 投票数量
        type: integer
    type: object
  response.PointsDetail:
    properties:
      amount:
        description: token数量
        example: "0.000182"
        type: string
      points:
        description: 积分
        example: "0.000182"
        type: string
      timestamp:
        description: 时间戳
        example: 1740840671
        type: integer
      type:
        description: 类型
        example: 1
        type: integer
    type: object
  response.PointsDetailsResponse:
    properties:
      cursor:
        description: 游标
        type: integer
      details:
        description: 积分明细列表
        items:
          $ref: '#/definitions/response.PointsDetail'
        type: array
      has_more:
        description: 是否还有更多
        type: boolean
    type: object
  response.PointsEstimatedResponse:
    properties:
      estimated_points:
        description: 预估积分
        example: "12862.90277"
        type: string
    type: object
  response.PointsResponse:
    properties:
      available_points:
        description: 可用积分
        example: "0.147938"
        type: string
      invite_points:
        description: 邀请积分
        example: "0"
        type: string
      invite_rebate:
        description: 可提取返佣
        example: "0.147938"
        type: string
      trading_points:
        description: 交易积分
        example: "0.147938"
        type: string
      withdrawable_rebate:
        description: 可提取返佣
        example: "0.147938"
        type: string
    type: object
  response.Response:
    properties:
      code:
        type: integer
      data: {}
      error:
        type: string
      msg:
        type: string
    type: object
  response.SearchTickerResponse:
    properties:
      cursor:
        description: 游标
        type: string
      has_more:
        description: 是否有更多数据
        type: boolean
      posts:
        description: 帖子列表
        items: {}
        type: array
      tickers:
        description: 市场列表
        items:
          $ref: '#/definitions/response.MarketResponse'
        type: array
      users:
        description: 用户列表
        items: {}
        type: array
    type: object
  response.SwapHistoriesResponse:
    properties:
      has_more:
        description: 是否还有更多数据
        type: boolean
      transaction_histories:
        description: 交易历史记录列表
        items:
          $ref: '#/definitions/response.TransactionHistory'
        type: array
    type: object
  response.TickerItem:
    properties:
      market:
        allOf:
        - $ref: '#/definitions/response.Market'
        description: 市场信息
      market_metadata:
        allOf:
        - $ref: '#/definitions/response.MarketMetadata'
        description: 市场元数据
      market_ticker:
        allOf:
        - $ref: '#/definitions/response.MarketTicker'
        description: 市场行情
    type: object
  response.TickersResponse:
    properties:
      list:
        description: 市场列表
        items:
          $ref: '#/definitions/response.TickerItem'
        type: array
    type: object
  response.TokenBalance:
    properties:
      balance:
        type: string
      decimals:
        type: integer
      owner:
        type: string
      token:
        type: string
    type: object
  response.TokenDistributionResponse:
    description: 代币持有者数据
    properties:
      token_holders:
        description: 代币持有者列表
        items:
          $ref: '#/definitions/response.TokenHolder'
        type: array
    type: object
  response.TokenHolder:
    description: 代币持有者详情
    properties:
      account:
        description: 持有者钱包地址
        example: 669VYcBRq51iQzFiPTQcsW2CsvLfHM9AwVmaoM1mAAR7
        type: string
      amount:
        description: 持有代币的数量
        type: string
      is_associated_bonding_curve:
        description: 是否与 Bonding Curve 关联
        example: false
        type: boolean
      is_black_hole:
        description: 是否为黑洞地址
        example: false
        type: boolean
      is_community_vault:
        description: 是否为社区金库
        example: false
        type: boolean
      moderator:
        allOf:
        - $ref: '#/definitions/response.Moderator'
        description: 持有者的管理员信息
      percentage:
        description: 持有代币的百分比
        example: "100.000000"
        type: string
      ui_amount:
        description: 代币的 UI 显示数量
        type: number
      user_profile:
        description: 用户资料（可能为空）
    type: object
  response.TokenHolding:
    properties:
      balance:
        description: 持仓数量
        type: string
      filled_price:
        description: 成交价格
        type: string
      holders_count:
        description: 持有者数量
        type: integer
      id:
        description: 代币 ID
        type: integer
      image_uri:
        description: 代币图片 URI
        type: string
      market_address:
        description: 市场地址
        type: string
      market_id:
        description: 市场 ID
        type: integer
      price:
        description: 当前价格
        type: string
      realized_pnl:
        description: 已实现盈亏
        type: string
      symbol:
        description: 代币符号
        type: string
      token_name:
        description: 代币名称
        type: string
      total_buy:
        description: 总买入数量
        type: string
      total_buy_native:
        description: 总买入原生代币数量
        type: string
      total_sell:
        description: 总卖出数量
        type: string
      total_sell_native:
        description: 总卖出原生代币数量
        type: string
      total_value:
        description: 持仓总价值
        type: string
    type: object
  response.TokenHoldingHistoriesResponse:
    properties:
      has_more:
        description: 是否有更多数据
        type: boolean
      token_holdings_histories:
        description: 代币持仓历史列表
        items:
          $ref: '#/definitions/response.TokenHoldingHistory'
        type: array
    type: object
  response.TokenHoldingHistory:
    properties:
      balance:
        description: 持仓数量
        type: string
      filled_price:
        description: 成交价格
        type: string
      holders_count:
        description: 持有者数量
        type: integer
      id:
        description: 记录 ID
        type: integer
      image_uri:
        description: 代币图片 URI
        type: string
      market_address:
        description: 市场地址
        type: string
      market_id:
        description: 市场 ID
        type: integer
      price:
        description: 当前价格
        type: string
      realized_pnl:
        description: 已实现盈亏
        type: string
      symbol:
        description: 代币符号
        type: string
      token_name:
        description: 代币名称
        type: string
      total_buy:
        description: 总买入数量
        type: string
      total_buy_native:
        description: 总买入原生代币数量
        type: string
      total_sell:
        description: 总卖出数量
        type: string
      total_sell_native:
        description: 总卖出原生代币数量
        type: string
      total_value:
        description: 持仓总价值
        type: string
    type: object
  response.TokenHoldingsResponse:
    properties:
      token_holdings:
        description: 代币持仓列表
        items:
          $ref: '#/definitions/response.TokenHolding'
        type: array
    type: object
  response.TransactionHistory:
    properties:
      block_time:
        description: 区块时间
        type: integer
      native_amount:
        description: 原生代币数量
        type: string
      payer:
        description: 支付方地址
        type: string
      signature:
        description: 交易签名
        type: string
      token_amount:
        description: Index        int    `json:"index"`         // 交易索引
        type: string
      token_price:
        description: 代币价格
        type: string
      trade_type:
        description: 交易类型：1=买，2=卖，3=回购
        type: integer
    type: object
  response.UserInfo:
    description: 包含用户 ID、地址、昵称、头像和描述等信息。
    properties:
      address:
        type: string
      invite_code:
        example: ""
        type: string
      twitter_id:
        type: string
      twitter_username:
        type: string
      type:
        type: integer
      user_id:
        type: integer
    type: object
host: v4-api.frogswap.org
info:
  contact: {}
  description: This is a sample API for demonstration purposes.
  title: Game API
  version: "1.0"
paths:
  /api/v1/ws/kline/{tokenAddress}:
    get:
      consumes:
      - application/json
      description: |-
        通过 WebSocket 实时推送 K 线数据。连接成功后，服务器将每秒推送最新的K线数据。
        连接格式: ws://host/api/v1/ws/kline/{tokenAddress}?resolution={resolution}
        示例(Solana): ws://localhost:4881/api/v1/ws/kline/8iFREvVdmLKxVeibpC5VLRr1S6X5dm7gYR3VCU1wpump?resolution=1S
        连接成功后，可以发送 "subscribe" 消息来开始接收数据
      parameters:
      - description: 代币地址
        example: 8iFREvVdmLKxVeibpC5VLRr1S6X5dm7gYR3VCU1wpump
        in: path
        name: tokenAddress
        required: true
        type: string
      - description: K线周期
        enum:
        - 1S
        - "1"
        - "5"
        - "15"
        - "60"
        - "240"
        - "720"
        - 1D
        example: 1S
        in: query
        name: resolution
        required: true
        type: string
      produces:
      - application/json
      responses:
        "101":
          description: WebSocket连接成功后的推送数据格式
          schema:
            allOf:
            - $ref: '#/definitions/response.KlineData'
            - properties:
                close:
                  type: string
                high:
                  type: string
                low:
                  type: string
                open:
                  type: string
                timestamp:
                  type: integer
                volume:
                  type: string
              type: object
      summary: K线数据 WebSocket 接口
      tags:
      - kline-data
  /global/{chain_type}/native_balance:
    get:
      consumes:
      - application/json
      description: 根据链类型和用户地址获取用户的钱包原生代币余额。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回原生代币余额
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.TokenBalance'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      security:
      - ApiKeyAuth: []
      summary: 获取链的原生代币钱包余额
      tags:
      - 全局数据
  /global/{chain_type}/native_token_price:
    get:
      consumes:
      - application/json
      description: 根据链类型获取原生代币的 USD 价格，保留 8 位小数
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回原生代币价格
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  additionalProperties:
                    type: string
                  type: object
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 获取链的原生代币价格
      tags:
      - 全局数据
  /global/{chain_type}/ticker_balance/{ticker_address}:
    get:
      consumes:
      - application/json
      description: 根据链类型和用户地址获取用户的钱包原生代币余额。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 代币地址
        in: path
        name: ticker_address
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回原生代币余额
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.TokenBalance'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      security:
      - ApiKeyAuth: []
      summary: 获取链的原生代币钱包余额
      tags:
      - 全局数据
  /klines/{klineType}/{chainType}/{tokenAddress}:
    get:
      consumes:
      - application/json
      description: Get historical kline (candlestick) data for a specific token
      parameters:
      - description: Kline type (kline for price data, mcapkline for market cap data)
        enum:
        - kline
        - mcapkline
        in: path
        name: klineType
        required: true
        type: string
      - description: Chain type
        enum:
        - sol
        - eth
        - bsc
        in: path
        name: chainType
        required: true
        type: string
      - description: Token address
        in: path
        name: tokenAddress
        required: true
        type: string
      - description: Resolution of kline data
        enum:
        - 1S
        - "1"
        - "5"
        - "15"
        - "60"
        - "240"
        - "720"
        - 1D
        in: query
        name: resolution
        required: true
        type: string
      - description: Start timestamp in seconds
        in: query
        name: from
        required: true
        type: integer
      - description: End timestamp in seconds
        in: query
        name: till
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Success
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  items:
                    $ref: '#/definitions/response.KlineData'
                  type: array
              type: object
        "400":
          description: Invalid parameters
          schema:
            $ref: '#/definitions/response.Response'
        "500":
          description: Server error
          schema:
            $ref: '#/definitions/response.Response'
      summary: Get token kline data
      tags:
      - kline-data
  /points/{chain_type}:
    get:
      consumes:
      - application/json
      description: 根据链类型和用户 ID 获取用户的交易积分、邀请积分和可用积分。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回用户积分数据
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.PointsResponse'
              type: object
        "401":
          description: 未授权
          schema:
            $ref: '#/definitions/response.Response'
      security:
      - ApiKeyAuth: []
      summary: 获取用户积分数据
      tags:
      - 积分
  /points/{chain_type}/detail:
    get:
      consumes:
      - application/json
      description: 根据链类型和用户 ID 获取用户的积分明细数据。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 游标
        in: query
        name: cursor
        type: string
      - description: 每页数量
        in: query
        name: limit
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回用户积分明细
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.PointsDetailsResponse'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      security:
      - ApiKeyAuth: []
      summary: 获取用户积分明细
      tags:
      - 积分
  /points/{chain_type}/estimated:
    get:
      consumes:
      - application/json
      description: 根据链类型和用户 ID 获取用户的预估积分数据。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回用户预估积分数据
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.PointsEstimatedResponse'
              type: object
        "401":
          description: 未授权
          schema:
            $ref: '#/definitions/response.Response'
      security:
      - ApiKeyAuth: []
      summary: 获取用户预估积分数据
      tags:
      - 积分
  /points/{chain_type}/invite/detail:
    get:
      consumes:
      - application/json
      description: 根据链类型和用户 ID 获取用户的积分统计数据。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 游标
        in: query
        name: cursor
        type: string
      - description: 每页数量
        in: query
        name: limit
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回用户积分明细
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.InvitedPointsTotalResponse'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      security:
      - ApiKeyAuth: []
      summary: 获取用户邀请明细
      tags:
      - 用户
  /swap/{chain_type}/get_transaction:
    get:
      consumes:
      - application/json
      description: 根据链类型、交易类型和请求参数获取 Swap 路由。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 交易类型（buy 或 sell）
        in: path
        name: tradeType
        required: true
        type: string
      - description: 代币地址
        in: query
        name: token_address
        required: true
        type: string
      - description: 发送地址（用户地址）
        in: query
        name: from_address
        required: true
        type: string
      - description: 输入代币地址
        in: query
        name: token_in_address
        required: true
        type: string
      - description: 输出代币地址
        in: query
        name: token_out_address
        required: true
        type: string
      - description: 输入代币所在链（sol、eth、bsc）
        in: query
        name: token_in_chain
        required: true
        type: string
      - description: 输出代币所在链（sol、eth、bsc）
        in: query
        name: token_out_chain
        required: true
        type: string
      - description: 输入金额（单位：最小代币单位，如 lamports、wei）
        in: query
        name: in_amount
        required: true
        type: string
      - description: 交易优先费（单位：最小代币单位，如 lamports）
        in: query
        name: priority_fee
        type: integer
      - description: 滑点（100 * 100 代表 1%）
        in: query
        name: slippage
        required: true
        type: string
      - description: 是否启用 Anti-MEV（默认 false）
        in: query
        name: is_anti_mev
        type: boolean
      - description: 是否使用 Legacy 交易模式（默认 false）
        in: query
        name: legacy
        type: boolean
      - description: 交易方向（buy 或 sell，可选）
        in: query
        name: swap_type
        type: string
      - description: 积分数（用于 g_points 交易类型）
        in: query
        name: points
        type: integer
      - description: 交易平台类型（pump、raydium、game、g_external、g_points）
        in: query
        name: platform_type
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回 Swap 路由信息
          schema:
            $ref: '#/definitions/response.Response'
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 获取 Swap 路由
      tags:
      - Swap
  /swap/{chain_type}/send_transaction:
    post:
      consumes:
      - application/json
      description: 根据链类型和 Swap 交易数据发送 Swap 请求。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: Swap 交易数据（Base64 编码）
        in: query
        name: swap_transaction
        required: true
        type: string
      - description: 是否启用 Anti-MEV（默认为 false）
        in: query
        name: is_anti_mev
        type: boolean
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回交易结果
          schema:
            $ref: '#/definitions/response.Response'
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 发送 Swap 请求
      tags:
      - Swap
  /swap/{chain_type}/transaction_status:
    get:
      consumes:
      - application/json
      description: 根据链类型和交易签名获取 Swap 请求状态。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 交易签名
        in: query
        name: swap_transaction
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回交易状态
          schema:
            $ref: '#/definitions/response.Response'
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 获取 Swap 请求状态
      tags:
      - Swap
  /tickers/{chain_type}:
    get:
      consumes:
      - application/json
      description: 根据链类型、排序、分页和搜索条件获取市场行情数据。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 排序字段，支持以下值：MARKET_CAP, PRICE_CHANGE_5M, PRICE_CHANGE_1H, PRICE_CHANGE_24H,
          NATIVE_VOLUME_1H, NATIVE_VOLUME_24H, TX_COUNT_24H, HOLDERS, INITIALIZE_AT,
          LINKS
        enum:
        - MARKET_CAP
        - PRICE_CHANGE_5M
        - PRICE_CHANGE_1H
        - PRICE_CHANGE_24H
        - NATIVE_VOLUME_1H
        - NATIVE_VOLUME_24H
        - TX_COUNT_24H
        - HOLDERS
        - INITIALIZE_AT
        - LINKS
        - PRICE
        example: '"INITIALIZE_AT"'
        in: query
        name: sorted_by
        type: string
      - description: 排序方向，支持以下值：DESC, ASC
        enum:
        - DESC
        - ASC
        example: '"DESC"'
        in: query
        name: sort_direction
        type: string
      - description: 分页游标，用于分页查询
        example: '""'
        in: query
        name: page_cursor
        type: string
      - description: 每页返回的数据条数
        example: 50
        in: query
        name: limit
        type: integer
      - description: 搜索关键字，用于筛选数据
        example: '""'
        in: query
        name: search
        type: string
      - description: 新交易对的时间分辨率，例如 1D（1 天）
        enum:
        - 1D
        - 1H
        - 1M
        example: '"1D"'
        in: query
        name: new_pairs_resolution
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回市场行情数据
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.TickersResponse'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 获取市场行情数据
      tags:
      - 市场行情
  /tickers/{chain_type}/detail/{ticker_address}:
    get:
      consumes:
      - application/json
      description: 根据链类型和代币地址获取 Ticker 的详细信息。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 代币地址
        in: path
        name: ticker_address
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回 Ticker 详情
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.GetTickerResponse'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 获取 Ticker 详情
      tags:
      - 市场行情
  /tickers/{chain_type}/market/{ticker_address}:
    get:
      consumes:
      - application/json
      description: 根据链类型和代币地址获取 Ticker 的详细信息。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 代币地址
        in: path
        name: ticker_address
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回 Ticker 详情
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.GetTickerResponse'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 获取 Ticker 详情
      tags:
      - 市场行情
  /tickers/{chain_type}/search:
    get:
      consumes:
      - application/json
      description: 根据链类型、搜索参数、分页参数等条件搜索 Tickers。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 搜索参数（如代币名称或地址）
        in: query
        name: param
        required: true
        type: string
      - description: 分页大小
        in: query
        name: limit
        required: true
        type: string
      - description: 分页游标
        in: query
        name: cursor
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回 Tickers 列表
          schema:
            $ref: '#/definitions/response.SearchTickerResponse'
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 搜索 Tickers
      tags:
      - 市场行情
  /tickers/{chain_type}/statistic/{ticker_address}:
    get:
      consumes:
      - application/json
      description: 根据链类型和代币地址获取 Ticker 的详细信息。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 代币地址
        in: path
        name: ticker_address
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回 Ticker 详情
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.GetTickerResponse'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 获取 Ticker 获取平台币统计信息
      tags:
      - 市场行情
  /tickers/{chain_type}/swap_histories/{ticker_address}:
    get:
      consumes:
      - application/json
      description: 根据链类型和代币地址获取交易历史记录。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 代币地址
        in: path
        name: ticker_address
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回交易历史记录
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.SwapHistoriesResponse'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 获取指定 Ticker 的交易历史记录
      tags:
      - 市场行情
  /tickers/{chain_type}/token_distribution/{ticker_address}:
    get:
      consumes:
      - application/json
      description: 根据链类型和代币地址获取代币持有者的分布信息。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 代币地址
        in: path
        name: ticker_address
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回代币分布信息
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.TokenDistributionResponse'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 获取指定 Ticker 的代币分布信息
      tags:
      - 市场行情
  /token_holdings/{chain_type}/{account}:
    get:
      consumes:
      - application/json
      description: 根据链类型、用户账户和目标账户获取代币持仓数据。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 用户账户
        in: path
        name: account
        required: true
        type: string
      - description: 目标账户
        in: query
        name: account
        required: true
        type: string
      - default: "false"
        description: 是否包含零余额
        in: query
        name: allow_zero_balance
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回代币持仓数据
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.TokenHoldingsResponse'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 获取代币持仓数据
      tags:
      - 代币持仓
  /token_holdings/{chain_type}/histories/{account}:
    get:
      consumes:
      - application/json
      description: 根据链类型和用户账户获取代币持仓历史数据。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 用户账户
        in: path
        name: account
        required: true
        type: string
      - default: "0"
        description: 分页页码
        in: query
        name: page
        type: string
      - default: "20"
        description: 每页数量
        in: query
        name: limit
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回代币持仓历史数据
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.TokenHoldingHistoriesResponse'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 获取代币持仓历史数据
      tags:
      - 代币持仓
  /users/{chain_type}/invite/code:
    get:
      consumes:
      - application/json
      description: 根据链类型和用户 ID 获取用户的邀请码和邀请数量。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回用户邀请码信息
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.InviteCodeResponse'
              type: object
        "401":
          description: 未授权
          schema:
            $ref: '#/definitions/response.Response'
      security:
      - ApiKeyAuth: []
      summary: 获取用户邀请码信息
      tags:
      - 用户
  /users/{chain_type}/login:
    post:
      consumes:
      - application/json
      description: 通过钱包地址和签名进行登录。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance Smart
        Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      - description: 登录请求参数
        in: body
        name: login
        required: true
        schema:
          $ref: '#/definitions/request.LoginRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 登录成功
          schema:
            $ref: '#/definitions/response.LoginResponse'
        "500":
          description: 服务器内部错误
          schema:
            $ref: '#/definitions/response.Response'
      summary: 用户钱包登录
      tags:
      - 用户
  /users/{chain_type}/my_info:
    get:
      consumes:
      - application/json
      description: 根据链类型和 JWT Token 获取当前用户的详细信息。支持的链类型：sol（Solana）、eth（Ethereum）、bsc（Binance
        Smart Chain）。
      parameters:
      - description: 链类型（sol、eth、bsc）
        in: path
        name: chain_type
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功返回用户信息
          schema:
            allOf:
            - $ref: '#/definitions/response.Response'
            - properties:
                data:
                  $ref: '#/definitions/response.MyInfoResponse'
              type: object
        "401":
          description: 未授权
          schema:
            $ref: '#/definitions/response.Response'
      security:
      - ApiKeyAuth: []
      summary: 获取当前用户信息
      tags:
      - 用户
securityDefinitions:
  ApiKeyAuth:
    in: header
    name: Authorization
    type: apiKey
  BearerAuth:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
